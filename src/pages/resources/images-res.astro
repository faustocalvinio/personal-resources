---
import Layout from '../../layouts/Layout.astro';
import Card from '../../components/Card.astro';
const imagesItems = await Astro.glob('../../content/images-stuff/*.md')
---

<Layout title="Images Resources">
    <main>
        <h1 class="text-black text-center font-extrabold text-5xl dark:text-white ">Images Resources page</h1>
        <ul class="grid grid-cols-[repeat(auto-fit,minmax(400px,1fr));]"> 
            {imagesItems.map(item=>(
                <Card 
                    title={item.frontmatter.title}
                    description={item.frontmatter.description}
                    link={item.frontmatter.link}
                />
            ))}
        </ul>
    </main>
</Layout>