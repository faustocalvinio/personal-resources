---
import Layout from '../../layouts/Layout.astro';
import Card from '../../components/Card.astro';
const apiItems = await Astro.glob('../../content/apis/*.md')
---

<Layout title="APIS Resources">
    <main>
        <h1 class="text-black text-center font-extrabold text-5xl dark:text-white ">API page</h1>
        <ul class="grid grid-cols-[repeat(auto-fit,minmax(400px,1fr));]"> 
            {apiItems.map(item=>(
                <Card 
                    title={item.frontmatter.title}
                    description={item.frontmatter.description}
                    link={item.frontmatter.link}
                />
            ))}
        </ul>
    </main>
</Layout>